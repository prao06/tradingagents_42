"""Streamlit dashboard for TradingAgents (backtests + decision journal).

Run with:  uv run --with streamlit streamlit run dashboard/Home.py
The data layer (``dashboard.data``) is import-light and unit-tested separately.
"""
